# Append this section to ~/.dbt/profiles.yml
dbt_modeling_profile:
  outputs:
    dev:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: postgres
      pass: password
      dbname: db_dev
      schema: modeling
    prod:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: postgres
      pass: password
      dbname: db_prod
      schema: modeling
  target: dev

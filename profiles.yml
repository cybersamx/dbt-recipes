
# Append this section to ~/.dbt/profiles.yml
dbt_recipes_profile:
  outputs:
    dev:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: postgres
      pass: password
      dbname: db_dev
      schema: dbt_recipes
    prod:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: postgres
      pass: password
      dbname: db_prod
      schema: dbt_recipes
  target: dev
